<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="biz.intro">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="false"
        android:normalScreens="false"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".detalhescarimbo"></activity>
        <activity android:name=".detalhescupao" />
        <activity
            android:name=".cupaofav"
            android:screenOrientation="portrait" />
        <activity
            android:name=".cupaocarimbo"
            android:screenOrientation="portrait" />
        <activity
            android:name=".cupao"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FidelizarSIMNAO"
            android:screenOrientation="portrait" />
        <activity
            android:name=".qrcode"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CartoeNFidelizados"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SobreAPP"
            android:screenOrientation="portrait" />
        <activity
            android:name=".notificacoes"
            android:screenOrientation="portrait" />
        <activity
            android:name=".configuracoes"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden"
            android:usesCleartextTraffic="true"
            />
        <activity
            android:name=".wallet"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Direitosdeutilizacao"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Register"
            android:screenOrientation="portrait"
            android:usesCleartextTraffic="true"
            />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps"
            android:label="@string/title_activity_maps"
            android:screenOrientation="portrait" />
        <activity
            android:name=".login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>